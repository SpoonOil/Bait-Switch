/// @description 

// Inherit the parent event
event_inherited();

if (global.beatGame) {
    maxLevel = 4
    maxInfo = "That's all the bait I've got, plus today's lunch, a provolone and spinach sandwich. The starfish seem to love it."
}